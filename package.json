{
    "name": "os-aws-ivs-player",
    "version": "1.0.0",
    "description": "OutSystems Support Library for AWS IVS Player SDK",
    "main": "index.js",
    "scripts": {
        "build": "tsc",
        "lint": "eslint ./src/**/*.ts",
        "lintfix": "eslint --fix ./src/**/*.ts",
        "prettier": "prettier --config ./.prettierrc.json --write \"**/*.+(js|ts|css)\"",
        "setup": "npm i && tsc",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "Stefan Weber",
    "license": "BSD-3-Clause",
    "devDependencies": {
        "amazon-ivs-player": "^1.19.0",
        "typescript": "^4.4.4"
    }
}
